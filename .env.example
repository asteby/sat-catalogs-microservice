# Database configuration
# Note: DB_HOST should be 'localhost' for local development, 
# but 'db' (service name) is used internally by docker-compose for the app service.
DB_HOST=localhost
DB_PORT=3306

# Generic Database Variables
DB_NAME=sat_catalogs
DB_USER=user
DB_PASSWORD=password

# Optional: Run migrations automatically on startup
AUTO_MIGRATE=false
